#Action to deploy directly to a domain
name: Deploy
on:
    push:
    workflow_dispatch:
jobs:
  deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
      
    - name: 🗜️ Minify
      uses: nizarmah/auto-minify@v2.1
      
    - name: ✉️ Deploy to subdomain
      if: true
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: greenvisionmedia.net
        remote-user: u1887-ynfzoamngj75
        private-ssh-key: ${{secrets.TEST_KEY}}
        destination-path: gv.greenvisionmedia.net/public_html/
        
    - name: 📩 Deploy to custom domain
      if: false
      uses: SamKirkland/web-deploy@v1
      with:
        target-server: greenvision.media
        remote-user: u1258-ihb3f9yhod3w
        private-ssh-key: ${{secrets.SSH_KEY}}
        destination-path: greenvision.media/public_html/
