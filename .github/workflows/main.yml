#Action to deploy directly to a domain and call re-usable deploy
name: Deploy
on:
    pull_request_target:
      types:
        - edited
        - opened
jobs:
  deploy:
    uses: greenvisionmedia/gvm-pipeline/.github/workflows/deploy.yml@d7db7895a4237f4ef24c66e222146d4bae0464b6
    with:
      tag: $GITHUB_BASE_REF
    secrets: inherit
    inputs:
      staging: ${{inputs.staging}}
      sitecode: ${{inputs.sitecode}}
      domain: ${{inputs.domain}}
